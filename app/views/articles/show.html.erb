<p>
  <strong>Título:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Texto:</strong>
  <%= @article.text %>
</p>

<%= link_to 'editar', edit_article_path(@article) %>
<%= link_to 'voltar', articles_path %>
<%= link_to 'apagar', article_path(@article),
    method: :delete,
    data: { confirm: 'tem certeza que deseja apagar o post?' } %>